<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

    <authentication-manager>
        <authentication-provider>
            <!--<password-encoder hash="bcrypt" />-->
            <jdbc-user-service data-source-ref="dataSource"
                                        users-by-username-query=
                                                "select id,userName,passWord from customers where userName=?"
                                        authorities-by-username-query=
                                                "select role_ID,role_Name from role r join customers c on r.role_ID=c.roleID where c.userName=?"/>
        </authentication-provider>

    </authentication-manager>

    <http pattern="/resources/**" security="none" />

    <http use-expressions="true">
<!--        <intercept-url pattern="/account/**" access="hasRole('ROLE_USER')"/>-->
        <intercept-url pattern="/account/edit" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/account/change" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/order/**" access="hasRole('ROLE_USER')"/>
<!--        <intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')"/>-->

        <!-- access denied page -->
        <access-denied-handler error-page="/403"/>
        <form-login
                login-page="/account/login"
                authentication-failure-url="/account/login"
                username-parameter="username"
                password-parameter="password"/>
    </http>
    <beans:bean id="encoder"
                class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="11" />
    </beans:bean>
</beans:beans>